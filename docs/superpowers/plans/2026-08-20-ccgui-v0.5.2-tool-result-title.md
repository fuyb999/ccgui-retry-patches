# CC GUI v0.5.2 Tool-Result Title Plan

1. Add a failing WebView regression test for a message window beginning with a
   structured `[tool_result]` carrier.
2. Reuse the shared tool-result carrier predicate in session-title selection.
3. Bump the patched plugin and artifact to `0.5.2-retry.7` and add upstream
   hashes for the newly patched WebView files.
4. Regenerate the version-specific patch from pinned upstream source.
5. Run focused and full tests, build the plugin with JDK 17, verify the ZIP and
   checksum, and retain the previous artifact.
6. Commit the source patch and release artifact separately on `main`.
